import React from "react";

const TableFunctionBar = ({ schema, userSettings }) => {
  // TODO
  // add record
  // export to csv
  // filter and search
  // toggle hidden columns including delete and edit
  return <div>TableFunctionBar</div>;
};

export default TableFunctionBar;
